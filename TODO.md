- Include small toggle button that will include the cards from `blockchain.txt`
    - this should merge those entries in to the main list in JS such that the randomness and UI indexing still appear

- Serve via GH pages
